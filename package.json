{
  "name": "mini-pos",
  "version": "1.0.0",
  "main": "dist/electron/main.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tsc -p tsconfig.json && tsc -p tsconfig.renderer.json && cp src/database/*.sql dist/src/database/",
    "start": "npm run build && electron .",
    "dev": "tsc --watch",
    "dev:server": "node dist/src/server.js",
    "test:cli": "npm run build && node dist/src/main.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "@types/better-sqlite3": "^7.6.13",
    "electron": "^43.0.0",
    "typescript": "^6.0.3"
  },
  "dependencies": {
    "@picocss/pico": "^2.1.1",
    "@types/cors": "^2.8.19",
    "@types/express": "^5.0.6",
    "better-sqlite3": "^12.11.1",
    "cors": "^2.8.6",
    "express": "^5.2.1"
  }
}
